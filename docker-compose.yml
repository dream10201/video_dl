services:
  video-dl:
    build: .
    image: ghcr.io/dream10201/video_dl:local
    ports:
      - "8080:8080"
    volumes:
      - ./downloads:/app/downloads
    environment:
      API_TOKEN: ${API_TOKEN:?set API_TOKEN}
      WORKERS: "1"
